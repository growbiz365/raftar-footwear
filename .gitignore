# --- Environment files (never commit secrets) ---
.env
.env.*
!.env.example

# --- Node / build ---
node_modules/
dist/
build/
.vite/

# Pre-built React app is intentionally committed so Hostinger never runs Vite
!frontend/dist/

# --- Logs ---
*.log
npm-debug.log*